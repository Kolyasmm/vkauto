# 🚀 Пошаговый деплой на Timeweb VPS

## Часть 1: Подготовка на Timeweb (5 минут)

### 1. Зарегистрируйтесь на Timeweb

Откройте: https://timeweb.com/ru/

### 2. Закажите VPS

1. Перейдите: https://timeweb.com/ru/services/vps
2. Выберите конфигурацию:

**Рекомендуемая:**
- **Тариф:** Start-2 или Start-3
- **CPU:** 2 ядра
- **RAM:** 2 GB
- **SSD:** 20 GB
- **ОС:** Ubuntu 22.04 LTS ⚠️ ВАЖНО!
- **Стоимость:** ~400₽/месяц

3. Оформите заказ
4. Подождите 5-10 минут (придет email)

### 3. Получите данные доступа

**В email от Timeweb будет:**
- IP адрес: например `123.45.67.89`
- Логин: `root`
- Пароль: случайная строка

**Сохраните эти данные!**

---

## Часть 2: Подключение к серверу (3 минуты)

### На Mac (у вас):

Откройте Терминал и выполните:

```bash
ssh root@ваш-ip-адрес
# Например: ssh root@123.45.67.89

# Введите пароль из email (не будет виден при вводе - это нормально)
# Нажмите Enter
```

При первом подключении появится:
```
The authenticity of host '...' can't be established.
Are you sure you want to continue connecting (yes/no)?
```

Напишите: `yes` и нажмите Enter

**✅ Вы подключены к серверу!**

---

## Часть 3: Загрузка проекта на сервер (5 минут)

### Способ 1: Через SCP (с вашего Mac)

Откройте **НОВОЕ окно терминала** (НЕ закрывайте SSH соединение):

```bash
cd /Users/nikolajorehov/Downloads
scp -r vkauto root@ваш-ip:/root/
```

Введите пароль от сервера.

Подождите загрузки (~1-2 минуты).

### Способ 2: Через Git (если загрузили на GitHub)

В SSH терминале на сервере:

```bash
cd /root
git clone https://github.com/ваш-username/vk-automation-platform.git
mv vk-automation-platform vkauto
cd vkauto
```

---

## Часть 4: Настройка переменных (10 минут)

На сервере (в SSH терминале):

```bash
cd /root/vkauto
cp .env.production .env
nano .env
```

### Откроется текстовый редактор. Заполните:

```env
# 1. Придумайте СЛОЖНЫЙ пароль для базы данных
POSTGRES_PASSWORD=Vm8kP2xQ9nR5tY1w

# 2. Вставьте ваш VK токен
VK_ACCESS_TOKEN=vk1.a.ваш_длинный_токен_здесь
VK_AD_ACCOUNT_ID=123456789

# 3. Придумайте случайную строку для JWT (минимум 32 символа)
JWT_SECRET=aB3dE5fG7hJ9kL1mN3pQ5rS7tU9vW1xY3zA

# 4. Укажите IP вашего сервера
NEXT_PUBLIC_API_URL=http://123.45.67.89

# 5. Telegram (если есть бот)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_NOTIFICATIONS_ENABLED=true

# Остальное можно оставить как есть
```

### Как сохранить в nano:

1. **Ctrl + X** (выйти)
2. Появится вопрос "Save modified buffer?" - нажмите **Y**
3. Подтвердите имя файла - нажмите **Enter**

**✅ Файл сохранен!**

---

## Часть 5: Автоматический деплой (15 минут)

Выполните на сервере:

```bash
cd /root/vkauto
chmod +x deploy.sh
./deploy.sh
```

### Что произойдет:

1. ✅ Установится Docker и Docker Compose (~3 мин)
2. ✅ Соберутся Docker образы (~5 мин)
3. ✅ Запустятся контейнеры (PostgreSQL, Redis, API, Web, Nginx)
4. ✅ Выполнятся миграции базы данных
5. ✅ Система будет готова к работе!

**Просто дождитесь завершения скрипта.**

---

## Часть 6: Проверка работы (2 минуты)

### 1. Проверьте статус контейнеров:

```bash
docker compose -f docker-compose.prod.yml ps
```

Должно быть примерно так:
```
NAME                    STATUS
vk-automation-api       Up
vk-automation-nginx     Up
vk-automation-postgres  Up (healthy)
vk-automation-redis     Up (healthy)
vk-automation-web       Up
```

Все должны быть **Up** (работают)!

### 2. Откройте в браузере:

```
http://ваш-ip-адрес
```

Например: `http://123.45.67.89`

**🎉 Вы должны увидеть веб-интерфейс VK Automation Platform!**

---

## Часть 7: Первое использование (5 минут)

### 1. Создайте правило:

1. Перейдите: "Правила" → "Создать правило"
2. Заполните:
   - **Название:** Автодублирование успешных групп
   - **CPL:** 200
   - **Минимум лидов:** 3
   - **Копий:** 3
   - **Время:** 09:00
3. Нажмите "Создать"

### 2. Протестируйте:

1. Найдите созданное правило
2. Нажмите иконку колбы (🧪 "Тестировать")
3. Система покажет сколько групп подходят БЕЗ создания копий

### 3. Активируйте:

Если тест прошел успешно - правило уже активно и будет выполняться каждый день в 09:00 МСК!

---

## 🎯 Готово! Система работает 24/7

### Полезные команды:

```bash
# Посмотреть логи API
docker compose -f docker-compose.prod.yml logs -f api

# Посмотреть логи Frontend
docker compose -f docker-compose.prod.yml logs -f web

# Перезапустить всё
docker compose -f docker-compose.prod.yml restart

# Остановить
docker compose -f docker-compose.prod.yml down

# Запустить снова
docker compose -f docker-compose.prod.yml up -d
```

---

## 🔒 Безопасность (опционально, но рекомендуется)

### Настройте firewall:

```bash
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

---

## 💰 Стоимость

**VPS Timeweb Start-2:**
- 2 CPU, 2 GB RAM, 20 GB SSD
- ~400₽/месяц
- **Работает 24/7 без перерывов!**

---

## 📞 Поддержка

**Если что-то пошло не так:**

1. Проверьте логи: `docker compose -f docker-compose.prod.yml logs -f api`
2. Проверьте статус: `docker compose -f docker-compose.prod.yml ps`
3. Перезапустите: `docker compose -f docker-compose.prod.yml restart`

**Частые проблемы:**

**Контейнер не запускается:**
```bash
docker compose -f docker-compose.prod.yml logs имя-контейнера
```

**Забыли .env:**
```bash
nano .env
# Заполните переменные
docker compose -f docker-compose.prod.yml restart
```

---

## 🎉 Всё работает!

Теперь ваш VK Automation Platform:
- ✅ Работает 24/7 на Timeweb
- ✅ Автоматически выполняет правила по расписанию
- ✅ Создает копии успешных групп объявлений
- ✅ Отправляет уведомления в Telegram
- ✅ Доступен по IP адресу из любой точки мира

**Следующий шаг (опционально):**
- Купите домен и настройте HTTPS (инструкция в DEPLOY.md)
