# 🚀 Запуск локально для тестирования

## Быстрый способ посмотреть веб-интерфейс

### 1. Установите зависимости (если ещё не сделали)

```bash
cd /Users/nikolajorehov/Downloads/vkauto
npm install
```

### 2. Запустите PostgreSQL и Redis через Docker

**Если Docker установлен:**

```bash
docker compose up -d
```

**Если Docker НЕ установлен:**

Установите Docker Desktop:
- Mac: https://www.docker.com/products/docker-desktop/
- После установки запустите Docker Desktop
- Затем выполните: `docker compose up -d`

**Или используйте локальный PostgreSQL/Redis:**
- PostgreSQL: https://postgresapp.com/
- Redis: `brew install redis && brew services start redis`

### 3. Настройте .env для локального запуска

```bash
cp .env.example .env
```

Отредактируйте [.env](.env):

```env
# Для локального запуска можно использовать тестовые значения
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vk_automation?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379

# VK API - вставьте реальные токены
VK_ACCESS_TOKEN=ваш_токен
VK_AD_ACCOUNT_ID=ваш_id

JWT_SECRET=test-secret-key-change-in-production

API_PORT=4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Telegram (можно оставить выключенным)
TELEGRAM_BOT_TOKEN=
TELEGRAM_NOTIFICATIONS_ENABLED=false

SCHEDULER_ENABLED=true
NODE_ENV=development
```

### 4. Выполните миграцию базы данных

```bash
cd apps/api
npx prisma migrate dev
npx prisma generate
cd ../..
```

### 5. Запустите приложение

```bash
npm run dev
```

### 6. Откройте в браузере

```
http://localhost:3000
```

**Frontend:** http://localhost:3000
**API:** http://localhost:4000/api

## 🎨 Что вы увидите

### Главная страница (Dashboard)
- Статистика: активные правила, запуски, созданные копии
- Список активных правил с расписанием
- Последние выполнения правил

### Страница "Правила"
- Список всех правил автодублирования
- Кнопки: Создать, Редактировать, Удалить
- Тест правила (симуляция без создания копий)
- Ручной запуск
- Включение/выключение правила

### Создание правила
- Название правила
- Порог CPL (₽)
- Минимум лидов за день
- Количество копий
- Время запуска (ЧЧ:ММ)
- Чекбокс активности

### История выполнений
- Детальная информация о каждом запуске
- Успешные группы с метриками
- Ошибки при создании копий
- Фильтрация по правилам

### Настройки
- VK API конфигурация
- Telegram настройки
- Статус подключения к БД

## Дизайн

**Стек:**
- TailwindCSS для стилизации
- Lucide React для иконок
- Адаптивный дизайн
- Современный минималистичный UI

**Цветовая схема:**
- Основной: Indigo/Blue
- Успех: Green
- Ошибка: Red
- Предупреждение: Yellow
- Фон: Light gray

## Тестирование функций

### Создать тестовое правило:

1. Перейдите в "Правила"
2. Нажмите "Создать правило"
3. Заполните:
   - Название: "Тест"
   - CPL: 200
   - Лиды: 3
   - Копии: 3
   - Время: 14:00
4. Сохраните

### Протестировать:

1. Нажмите кнопку "Тестировать" (иконка колбы)
2. Система покажет сколько групп подходят БЕЗ создания копий

### Запустить вручную:

1. Нажмите кнопку "Запустить" (иконка Play)
2. Правило выполнится немедленно
3. Результат появится в "История"

## Остановка

```bash
# Ctrl + C в терминале где запущен npm run dev

# Остановить Docker
docker compose down
```

## Готово!

Теперь вы можете протестировать весь интерфейс локально перед деплоем на Timeweb! 🎉
