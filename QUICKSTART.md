# Быстрый запуск VK Automation Platform

## Шаг 1: Установка зависимостей (5 минут)

```bash
cd vkauto
npm install
```

## Шаг 2: Настройка .env (3 минуты)

Скопируйте и отредактируйте:

```bash
cp .env.example .env
```

Минимально необходимые настройки в [.env](.env):

```env
VK_ACCESS_TOKEN=ваш_vk_токен_здесь
VK_AD_ACCOUNT_ID=ваш_id_кабинета
JWT_SECRET=любая-случайная-строка-для-безопасности
```

**Как получить VK токен:**
1. Откройте https://ads.vk.com/
2. Настройки → API → Создать токен
3. Права: управление объявлениями

**Как узнать Ad Account ID:**
- Откройте кабинет VK Ads
- ID в URL: `ads.vk.com/hq/dashboard/ad_plan?act=ВАШЕ_ID`

## Шаг 3: Запуск базы данных (1 минута)

```bash
docker-compose up -d
```

Проверка:
```bash
docker-compose ps
# Оба контейнера должны быть "Up"
```

## Шаг 4: Миграция БД (2 минуты)

```bash
cd apps/api
npx prisma migrate dev --name init
npx prisma generate
cd ../..
```

## Шаг 5: Запуск приложения (1 минута)

```bash
npm run dev
```

Откройте:
- **Frontend**: http://localhost:3000
- **API**: http://localhost:4000/api

## Первое использование

1. Перейдите в раздел "Правила"
2. Создайте новое правило:
   - Название: "Тестовое правило"
   - CPL: 200₽
   - Мин. лидов: 3
   - Копий: 3
   - Время: 09:00

3. Нажмите "Тестировать" для проверки без создания копий

4. Если всё работает — активируйте правило!

## Telegram (опционально)

1. Напишите [@BotFather](https://t.me/BotFather)
2. `/newbot` → следуйте инструкциям
3. Скопируйте токен в `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_NOTIFICATIONS_ENABLED=true
```

4. Напишите боту `/start`
5. Получите Chat ID: `https://api.telegram.org/bot<TOKEN>/getUpdates`
6. Обновите в БД:

```sql
UPDATE users SET telegram_chat_id = ваш_chat_id WHERE id = 1;
```

## Проблемы?

### База не подключается
```bash
docker-compose restart postgres
docker-compose logs postgres
```

### Ошибка VK API
- Проверьте токен и права доступа
- Убедитесь, что токен не истек

### Правило не выполняется
```bash
# Проверьте переменную в .env
SCHEDULER_ENABLED=true
```

## Готово! 🎉

Система автоматически будет проверять группы объявлений каждый день в установленное время и создавать копии успешных связок.

---

**Время установки:** ~15 минут
**Поддержка:** README.md содержит полную документацию
